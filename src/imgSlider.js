export default function imgSlider(){
    const portfolioSliderContainer = document.getElementById('port-image-slider__carousel');

    // READ THIS #################################################
    // do the multiple slider from stackoverflow 
}